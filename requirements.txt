# Runtime dependencies for relational-reality.
# Install with:  ./setup.sh   (or:  pip install -r requirements.txt)
#
# Only four packages are needed — everything else is the Python standard
# library. Version floors are conservative; newer releases are fine.

numpy>=1.24
scipy>=1.10
numba>=0.58       # JIT kernels for graph growth (the one heavy dependency)
matplotlib>=3.7   # only used to render the shape histogram + heatmap
